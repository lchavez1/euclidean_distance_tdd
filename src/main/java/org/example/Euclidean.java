package org.example;

import java.util.ArrayList;

public class Euclidean {

    // The Euclidean distance is a math formula that calculate the distance
    // between 2 points.

    // The syntax of the formula is below:
    // d = √(xb - xa)2 + (yb - ya)2 ...
    // (xb - xa)2 sum of each dimension and then square of result

    // Create a solution that meet the criteria for the unit testing (located in test folder),
    // using the method "distance" below:

    public Double distance(Point a, Point b) {
        // Code
        return 0.0;
    }

    // Try to implement by yourself but if you need help, review the below comments:
    // Math.sqrt(Math.pow(b.getX() - a.getX(), 2) + Math.pow(b.getY() - a.getY(), 2));


}
